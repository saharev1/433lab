# Архитектура и план Backend-разработки для Claude

Привет, Claude! Ниже описан план по разработке бэкенда для проекта 433lab, составленный на основе требований пользователя. Твоя задача — спроектировать и реализовать серверную часть, а также интегрировать её со статическим фронтендом.

## Основной функционал

1. **Регистрация и Авторизация:**
   - Роли: `Admin` (администратор) и `User` (пользователь).
   - Возможность регистрации и входа для пользователей.
   - Админ должен иметь защищенный доступ к загрузке контента.

2. **Загрузка и хранение медиа (Admin):**
   - Администратор может загружать фото и видео в существующие разделы сайта ("Слушать", "Смотреть" и т.д.).
   - Все загружаемые файлы должны сохраняться в строго заданную директорию на сервере.
   - Структура каталогов для хранения: 
     - `/media/Photos/`
     - `/media/Videos/`

3. **Взаимодействие пользователей с контентом:**
   - **Лайки**: Пользователи могут ставить лайки на медиафайлы.
   - **Комментарии**: Пользователи могут оставлять комментарии под контентом.
   - **Избранное**: Должен быть реализован раздел "Избранные", в котором отображаются все медиафайлы, которые лайкнул текущий пользователь.

## Технические требования и интеграция

- **Стек технологий**: На твой выбор (например, Node.js + Express/FastAPI + PostgreSQL/MongoDB). Важно, чтобы сервис легко контейнеризировался.
- **Инфраструктура Docker**: Текущий проект развёрнут в Docker (через nginx на порту 6060). Бэкенд нужно будет добавить как отдельный сервис в `docker-compose.yml`, а nginx настроить как reverse-proxy для маршрутизации запросов (например, все `/api/*` отправлять на бэкенд).
- **API (REST)**: 
  - `POST /api/auth/register`, `POST /api/auth/login`
  - `POST /api/media/upload` (только для Admin)
  - `GET /api/media/:section` 
  - `POST /api/media/:id/like`, `POST /api/media/:id/comment`
  - `GET /api/user/favorites`

## Задачи для Claude (Backend Implementation)

Ниже представлен список задач на основе плана разработки бэкенда:

- [x] 1. Спроектировать схему базы данных (Users, Media, Likes, Comments) — `backend/db.js`, SQLite (better-sqlite3).
- [x] 2. Написать backend API — `backend/server.js` (Node.js + Express):
  - [x] Регистрация и авторизация (Admin, User) — JWT, bcrypt; админ создаётся из env `ADMIN_EMAIL`/`ADMIN_PASSWORD`.
  - [x] Загрузка файлов (фото/видео) в директории `/media/Photos/` и `/media/Videos/` (только для Admin).
  - [x] Получение списка медиа по разделам — `GET /api/media/:section`.
  - [x] Добавление лайков (toggle) и комментариев.
  - [x] Получение избранных медиафайлов пользователя — `GET /api/user/favorites`.
- [x] 3. Написать Dockerfile для бэкенда и настроить `docker-compose.yml` (web nginx + backend; БД — SQLite в volume `dbdata`, медиа — volume `media`).
- [x] 4. Обновить конфигурацию nginx — `nginx/default.conf`: проксирование `/api/` и `/media/` на backend:3000.
- [x] 5. Интегрировать API во фронтенд (`api.js` подключён в `index.html`):
  - [x] Логика формы входа и регистрации (+ пункт меню ВОЙТИ/ВЫЙТИ).
  - [x] Динамическая загрузка контента разделов «Слушать» и «Смотреть» с сервера (fallback на статику, пока раздел пуст).
  - [x] Функционал лайков (с перевернутой иконкой) и комментариев.
  - [x] Раздел "Избранные" (пункт меню виден после входа) + экран «Загрузка» для админа.

## Статус: выполнено (2026-07-17)

API проверен смоук-тестом локально: регистрация/вход, загрузка файла админом (в `/media/Photos/`), запрет загрузки для user (403), лайк, комментарий, избранное, раздача файла — всё работает.

Деплой: `docker compose up -d --build` (web слушает 6060, как раньше). Переменные окружения: `JWT_SECRET`, `ADMIN_EMAIL`, `ADMIN_PASSWORD` (дефолт `admin@433lab.local` / `admin433` — сменить в проде!).

## Новые задачи (Биография автора)
- [x] **Страница Биографии**: Создать отдельный экран или страницу, содержащую биографию автора будущих работ. Текст/контент можно пока сделать заглушкой (или загружать с бэкенда).
- [x] **Интеграция с логотипом**: На экране меню добавлен логотип-кнопка (у элемента ID `#menu-author-bio`). Нужно добавить обработчик клика по этому элементу, чтобы он переводил пользователя на экран биографии.

### Статус: выполнено (2026-07-18)

- Добавлен экран `#screen-author-bio` («ОБ АВТОРЕ») в `frontend/index.html`: логотип + текст-заглушка биографии, кнопка «назад» через `goBack()`.
- Клик по логотипу `#menu-author-bio` реализован через атрибут `data-target="screen-author-bio"` — навигацию обрабатывает существующий механизм в `app.js` (история переходов и анимации работают автоматически, отдельный JS-обработчик не нужен).
- Стили `.bio-container` / `.bio-logo` / `.bio-text` добавлены в `frontend/styles.css` (+ мобильная адаптация), cache-bust поднят до `styles.css?v=7`.
- Текст биографии — заглушка; когда появится настоящий текст, достаточно заменить содержимое `.bio-text` (при желании позже можно грузить с бэкенда).

## Новые задачи (Поиск и сохранение состояния навигации)

### 1. Индексация и Поиск
- **Поиск по тегам постов**: При добавлении медиа/поста через админ-панель должна быть возможность указать тег (теги). Необходимо добавить соответствующее поле в БД (например, в таблицу `Media`).
- При использовании строки поиска (на экране меню) должен производиться поиск по этим тегам (и названиям постов), выдавая соответствующие посты в результатах.
- **Поиск по разделам меню**: Если поисковый запрос совпадает (или содержит) название раздела (например, "Проза", "Слушать", "Смотреть"), поиск должен находить этот раздел, позволяя пользователю быстро перейти в него.
- Реализовать API эндпоинт для поиска (например, `GET /api/search?q=...`) и отображение результатов на фронтенде.

### 2. Сохранение текущего экрана при перезагрузке
- В данный момент при обновлении страницы (перезагрузке) состояние сбрасывается и пользователя перекидывает на главный стартовый экран.
- **Задача**: Сделать так, чтобы при перезагрузке страницы пользователь оставался на том экране/разделе, где он находился. 
- Для реализации можно использовать URL hash (например, `/#screen-listen`) или `sessionStorage`/`localStorage`. Фронтенд (`app.js`) при инициализации должен считывать это состояние и сразу отображать нужный экран, минуя стартовую заставку.

### Статус: выполнено (2026-07-18)

**Поиск:**
- БД: колонка `tags TEXT NOT NULL DEFAULT ''` в таблице `media` + автомиграция `ALTER TABLE` для существующих баз (`backend/db.js`).
- Бэкенд: теги принимаются при загрузке (`POST /api/media/upload`, поле `tags` — через запятую, нормализуются: пробелы/`#` убираются) и редактируются через `PATCH /api/media/:id`; отдаются массивом в поле `tags`. Новый эндпоинт `GET /api/search?q=...` — регистронезависимый поиск по названию, описанию и тегам (фильтрация в JS, т.к. LIKE/lower() SQLite не понимают кириллицу; на объёмах сайта это дёшево).
- Фронтенд: поле «Теги» в форме загрузки и в форме редактирования админа. Enter в любой строке поиска открывает экран `#screen-search`; на нём живой поиск при наборе (debounce 350мс). Результаты: совпавшие названия разделов (клик — переход в раздел) + посты (карточка с плеером/фото, лайком, комментариями, тегами и ссылкой на раздел). Совпадение разделов ищется по словарю SCREEN_NAMES на клиенте.

**Сохранение экрана:**
- `app.js`: текущий экран пишется в URL-хэш через `history.replaceState` (без засорения истории браузера) при каждом переходе и goBack; при загрузке страницы экран из хэша активируется сразу, минуя заставку (фон-дорога скрывается для светлых экранов). Навигация переведена на делегирование событий + добавлен `window.navigateTo(screenId)` для программных переходов.
- `api.js` при загрузке подтягивает контент восстановленного раздела (loadSection/loadFavorites).
- Ограничение: текст поискового запроса после перезагрузки не восстанавливается (экран поиска откроется пустым).

Проверено: смоук-тест бэкенда на :3997 (загрузка с тегами, нормализация, поиск по тегу/названию в разных регистрах, PATCH тегов, пустой запрос), скриншоты headless Chrome — `#screen-listen` и `#screen-search` восстанавливаются из хэша без заставки.

## Новые задачи (Добавление текстовых постов / статей для админа)

### Контекст
На данный момент панель загрузки (`#screen-admin`) поддерживает только загрузку медиафайлов (аудио, видео, фото) для ограниченного списка разделов. Невозможно опубликовать текстовую статью, прозу, стихи, новость или идею без обязательного файла.

### 1. Бэкенд (БД и API)
- **Схема БД (`backend/db.js`)**:
  - Обновить таблицу `media`: добавить поддержку типа `text` в колонку `type` (варианты: `photo`, `video`, `audio`, `text`).
  - Добавить колонку `text_content TEXT DEFAULT ''` для хранения основного текста поста/статьи. Реализовать автомиграцию (`ALTER TABLE media ADD COLUMN text_content TEXT DEFAULT ''`).
- **API эндпоинты (`backend/server.js`)**:
  - `POST /api/media/upload`: сделать загрузку файла опциональной (multer не должен возвращать ошибку 400, если файл отсутствует, но заполнен текстовый контент `text_content`).
  - Если файл отсутствует, присваивать `type = 'text'` и `url = ''` (или путь к прикрепленному обложечному изображению, если файл загружен).
  - Обновить `PATCH /api/media/:id`: разрешить редактирование поля `text_content`.
  - Эндпоинты `GET /api/media/:section` и `GET /api/search` должны отдавать поле `text_content` в объекте ответа.

### 2. Фронтенд (Админ-панель `#screen-admin`)
- **Расширение списка разделов (`index.html`)**:
  - Добавить в выпадающий список `upload-section` все доступные разделы сайта (включая «Читать» / проза, стихи, «Учить», «Идеи», «Новости» и т.д.).
- **Интерфейс формы загрузки (`index.html`, `styles.css`)**:
  - Добавить поле ввода для текста публикации (`<textarea class="form-input" id="upload-text" placeholder="Текст публикации / статьи (опционально)"></textarea>`).
  - Сделать загрузку файла необязательной (файловый инпут может оставаться для загрузки фото/иллюстрации к статье).
- **Отправка формы (`api.js`)**:
  - Передавать `text_content` в `FormData` при отправке `POST /api/media/upload`.
  - При редактировании поста админом (`PATCH`) выводить инлайн-редактирование с полем `text_content`.

### 3. Отображение текстовых постов
- **Рендеринг карточек текстовых постов (`api.js`, `styles.css`)**:
  - Реализовать отображение текстовых постов в разделах («Читать», «Учить», «Идеи», «Новости» и др.): заголовок, текст поста/статьи, прикрепленная иллюстрация (если есть), теги, кнопки лайков, комментарии, кнопки редактирования/удаления для админа.
  - Если текст очень длинный — предусмотреть сворачивание/разворачивание («Читать полностью»).
  - Подключить динамическую загрузку постов через `loadSection` для текстовых разделов.
  - Обеспечить корректное отображение текстовых постов в поиске (`#screen-search`) и в «Избранном».

### Статус: выполнено (2026-07-22)

**Бэкенд (`backend/db.js`, `backend/server.js`):**
- Схема `media`: тип `text` добавлен в CHECK, новая колонка `text_content TEXT NOT NULL DEFAULT ''`, `filename`/`url` стали `DEFAULT ''` (текстовый пост без файла). Так как SQLite не меняет CHECK через `ALTER TABLE`, для существующей прод-базы написана **безопасная одноразовая пересборка таблицы** (`foreign_keys=OFF` → создать `media_new` → скопировать данные → drop/rename → восстановить индекс), срабатывает по отсутствию колонки `text_content`. Смоук-тест подтвердил: старые медиа, лайки, комментарии (FK) и теги сохраняются.
- `POST /api/media/upload`: файл теперь опционален (`upload.single` без обязательной проверки). Логика типа: есть текст + нет файла → `type='text'`, `url=''`; есть текст + картинка → `type='text'` с картинкой-обложкой (в `Photos`); только файл → обычное медиа. Пустой пост (ни файла, ни текста) → 400.
- `PATCH /api/media/:id`: редактирование `text_content`. `DELETE`: каталог файла определяется по `url` (`/Photos/` vs `/Videos/`), для постов без файла удаление не трогает диск. `mediaWithMeta` отдаёт `textContent`.

**Фронтенд (`index.html`, `api.js`, `styles.css`):**
- Форма загрузки `#screen-admin`: выпадающий список разбит на `optgroup` и включает все текстовые разделы (Читать: стихи/проза/сценарии/сказки/мысли/статьи; Учить: гайды/статьи; Идеи). Добавлены `<textarea id="upload-text">` и подсказка; файл больше не обязателен.
- Созданы недостающие экраны, на которые вели «мёртвые» пункты меню: `screen-read-tales`, `screen-read-thoughts`, `screen-read-articles`, `screen-learn-articles`.
- `api.js`: текстовые разделы добавлены в `MEDIA_SECTIONS` (kind `text`), `SECTION_TITLES`, `SCREEN_NAMES`. Новый `renderText` (обложка, заголовок, текст со сворачиванием «Читать полностью» при длине >600, теги, лайк/коммент/админ-кнопки). `loadSection` рендерит `.article-list`. Форма редактирования получает `<textarea>` для текстовых постов; сохранение шлёт `text_content`. Загрузка требует файл ИЛИ текст. Поиск и «Избранное» рендерят текстовые посты через общий `searchMediaBlock`.
- Cache-bust: `styles.css?v=11`, `api.js?v=4`.

**Проверка:** смоук-тест бэкенда (19/19 PASS) — миграция старой схемы, текстовый пост без файла, отображение в разделе, PATCH текста, поиск по тегу, пост с обложкой, удаление с файлом и без, регрессия обычного медиа, отклонение пустого поста. Синтаксис JS — `node --check`. `app.js` показывает новые экраны обобщённо (без хардкода). НЕ задеплоено — на стороне Antigravity.

**Замечание для заказчика:** статичные стихи/проза из прототипа (напр. «Маленькая свинья») в разделах Читать теперь заменяются динамическим контентом с сервера — их нужно один раз добавить через панель «Загрузка», иначе раздел покажет «Пока пусто».
